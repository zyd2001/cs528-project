### Install gradio==4.44.1
### Must install pydantic==2.10.6
### `python3 server.py` to launch the server
### `python3 exploit.py "url"` for exploit

#### Some http responses are compressed so they are not readable (for example https://www.google.com)